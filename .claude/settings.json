{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"${CLAUDE_PROJECT_DIR:-/home/user/TC-drms}\" && [ \"$(git branch --show-current)\" = \"main\" ] && { git diff --quiet && git diff --cached --quiet || (git add -A && git commit -m \"auto-sync $(date -u +%FT%TZ)\" && git push origin main); } || true"
          }
        ]
      }
    ]
  }
}
