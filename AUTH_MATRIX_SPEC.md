# AUTH_MATRIX_SPEC — 全系統授權主矩陣（脊椎 B）

> **定位**：把散落在 `app.js` 二十餘處的權限規則，逆向收斂成一張可討論、可實作、可稽核的主矩陣。
> 本文件同時是：①工作坊**場次 B（TODO-09 授權矩陣）**的白板底稿、②前端重構的規格、③未來後端（脊椎 A）驗證層的同一份真相。
> `〖…〗` 標記 = **只有人能拍板的參數**，工作坊當天填。其餘皆為可先行的暫行預設。
>
> 對應 `FABLE_HANDOFF.md` 脊椎 B。撰於 2026-07-05。
>
> **實作狀態（2026-07-05）**：§8 的 `can()` 已落地於 `app.js`（非薄殼，直接讀 §4.3/§4.4/§4.5 資料並強制執行），
> §8.1 重構點清單中 F1/F2/F4/F5/F6 與 ACT-T3/W2-4/C2-3/S2-3/X1-2/X6 已完成遷移；§7 破窗機制已泛化並可從
> UI 觸發完整迴圈（申請→核准→授予→到期回收）；§6 三條硬 SoD（核准≠申請、驗收≠送貨、破窗審批≠申請中的前兩條）
> 已強制執行。25 項 Playwright 迴歸全通過。未完成：ACT-C4（個資分級守門，目前仍是 maskPII 二值遮蔽 + S3 欄位遮蔽，
> 尚未接上 can() 分級）、代理/逾時升級（§5）、§9 八項待場次 B 表決後才能定案的門檻值。

---

## 1. 為什麼是「一張表」而不是「到處寫 if」

現況：每個模組自己寫 `if(role==='admin'||...)`。這造成三類實際傷害（下節有證據）：

1. **答案不一致** — 同一個問題「戰時誰能進這頁」，導覽列和頁面守門給出不同答案。
2. **改不動** — 工作坊定案後，要改二十幾處散落 if，漏一處就是漏洞。
3. **無法稽核** — 拒絕存取沒有留痕，紅隊（脊椎 C）無從證明「守門有效」。

目標形態：**單一函式 `can(actor, action, context)`**，讀同一張矩陣；所有模組改為呼叫它；每次拒絕/破窗都自動寫入稽核流（脊椎 C 接點）。後端上線時（脊椎 A），同一張矩陣搬到伺服器端再驗一次——前端矩陣變成 UX 提示層，後端矩陣才是真守門。

---

## 2. 現況盤點 — 散落規則完整清單（證據）

### 2.1 集中式（已有雛形）
| # | 機制 | 位置 | 內容 |
|---|------|------|------|
| C1 | `PERMS_PEACE` / `PERMS_WAR` | app.js:5586 起 | 角色×模組 → P0~P4（P0 全權/P1 編輯/P2 修正/P3 閱讀/P4 無權） |
| C2 | `defaultPerms()` | :5588 | admin→P0；it→P0（僅平時）；vol→白名單 P3 其餘 P4；leader→黑名單 P4 其餘 P1；**其他角色→P1** |
| C3 | 權限矩陣 UI（`cycleP`） | :5969 | admin/it 可手動調格子 |

### 2.2 散落式（硬編碼，矩陣外自立門戶）
| # | 規則 | 位置 | 內容 | 型態 |
|---|------|------|------|------|
| S1 | `canViewFullPII()` | :4733 | admin/it 看完整個資，其餘遮蔽 | 二值 |
| S2 | `canAssignCase()` | :7906 | admin/staff/leader 可指派個案負責人 | 白名單 |
| S3 | `welfareApprovalLevel()` | :7723 | ≤3k 組長 / ≤10k 幹部 / >10k 高層 | **僅顯示用，未強制** |
| S4 | 金援核准擋 vol | :7753 | `advanceWelfare` 只擋 `role==='vol'` | 黑名單 |
| S5 | `rtGuardHighRiskAssign()` | :551 | 高風險任務派工 → confirm 覆核 + 稽核 | 軟守門 |
| S6 | `setS()` 戰時切換 | :4636 | admin/it 才能切平時/戰時 | 白名單 |
| S7 | `toggleEditMode()` | :4192 | admin 才能編輯儀表板 | 白名單 |
| S8 | `checkAdminGate()` | :4618 | 系統管理頁限 admin | 白名單 |
| S9 | `dispatchDynamicRoutePermission()` | :8660 | admin/it 才能核發動線金鑰 | 白名單 |
| S10 | `applyDevTasksVisibility()` | :8963 | admin/it 才看得到開發任務 widget | 白名單 |
| S11 | 戰時視圖攔截 | :4544 | 非 admin/it 在戰時強制進 war view | 硬跳轉 |
| S12 | `requestPerm`/`approvePerm` | :6566 | 臨時提權 30 秒自動回收，但**目標寫死 staff×vol_hub** | 破窗原型 |
| S13 | LOA 高風險攔截 | :2714, 2844 | 班長接高風險單 → 自動轉幹部覆核卡 | 流程守門 |
| S14 | `NAV_CFG[].roles` | :4439 起 | 每個導覽項有 roles 陣列——**但沒有任何程式讀它** | 死資料 |
| S15 | `verifyDelivery()` / `dispatchReq()` | :8420 起 | 物資派案與驗收**完全沒有角色檢查** | 缺門 |
| S16 | `rtLockTask()` | :1032 | 任何角色可鎖任務，lockedBy=當前 role | 缺門 |

### 2.3 盤點揭露的結構性發現（比單條規則更重要）

**F1｜權限 fallback 是「預設開放」。**
`(PERMS_PEACE[role]&&PERMS_PEACE[role][id])||'P1'` —— 查無此角色時給 **P1 編輯權**。上次 vol/leader 缺權限定義的 bug 正是這個 fallback 造成的，修掉了症狀，但病根還在：下次任何人加新角色而忘了進矩陣，又是全開。安全系統的 fallback 必須是 **P4（default-deny）**。

**F2｜戰時導覽鎖與頁面守門讀不同的表。**
`renderNav`（:4500）永遠讀 `PERMS_PEACE`；`showPage`（:4560）讀 `state==='war'?PERMS_WAR:PERMS_PEACE`。後果：戰時可能「導覽顯示可進，點進去卻 P4 擋下」（混亂），或反向「導覽上鎖但戰時矩陣其實開放」——**災害當下把人擋在有權限的功能外面，是可用性事故**。

**F3｜系統裡有三套角色詞彙，互不映射。**
- 儀表板角色：`admin / it / staff / logistics / vol / leader`（ROLES + 角色下拉）
- LOA 前線角色：`志工 / 班長 / 司機 / 香積 / 訪視 / 幹部`（LOA_ROLES_SPEC 收斂的 6 權限面）
- 組織職稱：`組長 / 幹部 / 高層`（`welfareApprovalLevel` 的用語）
金援核准層級說「幹部才能核 ≤10k」，但系統裡**根本沒有「幹部」這個角色**——所以 S3 只能是顯示文字，無法強制。主矩陣必須先統一詞彙（§4.1），否則工作坊討論出來的授權沒有著力點。

**F4｜金援級距未強制。**
五步鏈只擋了 vol 不能核准；3 萬元和 3 千元的核准，系統行為完全相同。級距軸（§4.4）要接上真正的 `can()` 檢查。

**F5｜物資動線整段無門。**
派案（把車和貨派出去）、驗收（確認收到多少）都沒有角色檢查。以工作坊語境說：**倉庫的錢和貨，比金援的錢更容易被前端誤觸**。驗收簽收人也未記錄身分（logSys 有寫，但 actor 是誰按的就是誰，無驗證）。

**F6｜個資遮蔽是二值的，但個資敏感度不是。**
`canViewFullPII` 只分「全看/遮蔽」。但實際欄位敏感度分層明顯：身分證 ≠ 電話 ≠ 姓名。更關鍵的是盤點時發現**兩個特種個資欄位目前完全沒遮蔽**：
- `diet`（「對甲殼類過敏」= 健康資訊，個資法特種資料）— 報名表全員可見
- `psych`（「需持續關懷」= 心理狀態）— 個案頁可見
香積組**需要**過敏資訊備餐、訪視組**需要**心理狀態接手——所以不能一遮了之，要按「角色×欄位類別」給（§4.5）。這是二值遮蔽做不到的。

---

## 3. 主矩陣的形狀：五個維度，但不是五維表

天真做法是做一張 `角色×動作×級距×情境×組織` 的五維表——沒人填得完也沒人看得懂。實際結構是**分層的**：

```
第一層  動作目錄（ACT-*）         —— 系統裡所有需要授權的動作，各給一個 ID
第二層  基準矩陣（平時）          —— 角色 × 動作 → 允許/級距上限/遮蔽等級
第三層  戰時差分（delta）         —— 只列「戰時和平時不同」的格子
第四層  情境修飾器               —— 代理、逾時升級、破窗、跨聯絡處
```

**戰時用差分而不是整份複製**，是刻意的設計決定：現行 `PERMS_WAR` 是整份獨立的表，和 `PERMS_PEACE` 各自演化——F2 的不一致就是這樣長出來的。差分表天然回答「戰時到底放寬了什麼」，這正是工作坊高層最想審的問題清單，也讓稽核（脊椎 C）能自動標記「此操作使用了戰時放寬」。

---

## 4. 主矩陣內容

### 4.1 角色統一映射表（先於一切）

| 統一角色 ID | 儀表板角色 | LOA 角色 | 組織職稱對應 | 授權位階 |
|---|---|---|---|---|
| `R-HQ` | admin（總控） | — | 〖高層？總指揮？〗 | 5 |
| `R-IT` | it | — | IT 志工 | 4（技術面 5，業務面〖?〗） |
| `R-OFFICER` | staff | 幹部 | 幹部 | 4 |
| `R-LEAD` | leader | 班長 | 組長 | 3 |
| `R-LOGI` | logistics | 司機 | 物資組 | 2 |
| `R-CARE` | —（缺） | 訪視 | 訪視員/社工 | 2 |
| `R-KITCHEN` | —（缺） | 香積 | 香積組 | 1 |
| `R-VOL` | vol | 志工 | 一般志工 | 1 |

發現：**訪視、香積在儀表板端沒有對應角色**——但金援五步鏈的「申請人」就該是訪視員（R-CARE）、過敏資訊的合法讀者就是香積（R-KITCHEN）。〖工作坊確認：訪視/香積要不要成為儀表板正式角色，還是永遠只活在 LOA 端？〗

**IT 的兩面性**：現行 it 在平時全模組 P0（等同 admin）。技術維運需要，但**業務面**（核金援、看完整個資）it 應不應該有權？這是內部濫用威脅（脊椎 A 威脅模型第 2 層）最典型的入口。暫行建議：it 保留系統面 P0，業務動作（ACT-W*、ACT-P*）降為需破窗。〖工作坊確認〗

### 4.2 動作目錄（ACT Catalog）

每個動作一個 ID。今後程式裡不再出現 `if(role===…)`，只出現 `can(actor,'ACT-T3',ctx)`。

**任務域（rtsync）**
| ID | 動作 | 現行位置 | 級距軸 |
|---|---|---|---|
| ACT-T1 | 建立任務 | rtWiz/rtInsertTask | — |
| ACT-T2 | 派工（指定志工） | rtDoAssign/rtAssignTask | 風險 |
| ACT-T3 | 派工高風險任務 | rtGuardHighRiskAssign | 風險=high |
| ACT-T4 | 確認接受/拒絕任務 | rtAcceptTask/rtRejectTask | —（限被指派人）|
| ACT-T5 | 回報完成/受阻 | rtReport/rtBlockedRelease | — |
| ACT-T6 | 強制置頂/鎖定/解鎖/重啟 | rtForceTop/rtLockTask/… | — |

**金援域（persons.welfare）**
| ID | 動作 | 現行位置 | 級距軸 |
|---|---|---|---|
| ACT-W1 | 申請金援 | applyWelfare | 金額 |
| ACT-W2 | 審核 | advanceWelfare(review) | 金額 |
| ACT-W3 | 核准 | advanceWelfare(approve) | **金額（強制）** |
| ACT-W4 | 發放 | advanceWelfare(disburse) | 金額 |
| ACT-W5 | 簽收確認 | advanceWelfare(receipt) | — |

**個案域（persons）**
| ID | 動作 | 現行位置 | 級距軸 |
|---|---|---|---|
| ACT-C1 | 建立個案 | reliefSpawnTaskAndCase | — |
| ACT-C2 | 指派/改派負責人 | doAssignCase | — |
| ACT-C3 | 推進階段/結案 | advancePersonPhase/closePersonCase | — |
| ACT-C4 | 讀個資欄位 | maskPII 各呼叫點 | **個資敏感度** |

**物資域（warehouse/assets）**
| ID | 動作 | 現行位置 | 級距軸 |
|---|---|---|---|
| ACT-S1 | 叫料 | loaVolSupply 等 | — |
| ACT-S2 | 派案（出車出貨） | dispatchReq（**現無門**） | 〖金額/量？〗 |
| ACT-S3 | 到場驗收簽收 | verifyDelivery（**現無門**） | — |
| ACT-S4 | 資產調用/歸還/損壞判定 | createLoan/returnAsset | 高價值旗標 |

**系統域（admin/監控）**
| ID | 動作 | 現行位置 | 級距軸 |
|---|---|---|---|
| ACT-X1 | 切換平時/戰時 | setS | — |
| ACT-X2 | 編輯權限矩陣 | cycleP | — |
| ACT-X3 | 核准臨時提權（破窗審批） | approvePerm | — |
| ACT-X4 | 編輯儀表板/模組管理 | toggleEditMode | — |
| ACT-X5 | 匯出稽核/故事線/備份還原 | rtExportAudit/exportStoryline | — |
| ACT-X6 | 動線金鑰核發 | dispatchDynamicRoutePermission | — |

### 4.3 基準矩陣（平時）— 工作坊白板底稿

`✔`=允許 `✔ⁿ`=允許但有級距上限（見 4.4）`◐`=允許但觸發補償控制（見 §6）`✗`=拒絕 `⚡`=可破窗（見 §7）

| 動作 | R-HQ | R-IT | R-OFFICER | R-LEAD | R-LOGI | R-CARE | R-KITCHEN | R-VOL |
|---|---|---|---|---|---|---|---|---|
| ACT-T1 建任務 | ✔ | ✔ | ✔ | ✔ | ✗ | ✗ | ✗ | ✗ |
| ACT-T2 派工 | ✔ | ✔ | ✔ | ✔ | ✗ | ✗ | ✗ | ✗ |
| ACT-T3 派高風險 | ✔ | ◐ | ◐ | ✗⚡ | ✗ | ✗ | ✗ | ✗ |
| ACT-T4 接受/拒絕 | — | — | — | 限本人 | 限本人 | 限本人 | 限本人 | 限本人 |
| ACT-T6 置頂/鎖定 | ✔ | ✔ | ✔ | 限本班 | ✗ | ✗ | ✗ | ✗ |
| ACT-W1 申請 | ✔ | ✗ | ✔ | ✔ | ✗ | ✔ | ✗ | ✗ |
| ACT-W2 審核 | ✔ | ✗ | ✔ | ✔¹ | ✗ | ✗ | ✗ | ✗ |
| ACT-W3 核准 | ✔³ | ✗⚡ | ✔² | ✔¹ | ✗ | ✗ | ✗ | ✗ |
| ACT-W4 發放 | ✔ | ✗ | ✔ | ✗ | ✗ | ✗ | ✗ | ✗ |
| ACT-W5 簽收 | 見註 | — | 見註 | — | — | 代收◐ | — | — |
| ACT-C2 指派負責人 | ✔ | ✗ | ✔ | ✔ | ✗ | ✗ | ✗ | ✗ |
| ACT-C3 結案 | ✔ | ✗ | ✔ | ✗⚡ | ✗ | ✗ | ✗ | ✗ |
| ACT-S2 物資派案 | ✔ | ✔ | ✔ | ✗ | ✔ | ✗ | ✗ | ✗ |
| ACT-S3 驗收簽收 | ✔ | ✗ | ✔ | ✔ | ◐² | ✗ | ✗ | ✗ |
| ACT-X1 戰時切換 | ✔ | ✔ | ✗⚡ | ✗ | ✗ | ✗ | ✗ | ✗ |
| ACT-X2 編輯矩陣 | ✔ | ◐ | ✗ | ✗ | ✗ | ✗ | ✗ | ✗ |
| ACT-X3 破窗審批 | ✔ | ✗ | 〖?〗 | ✗ | ✗ | ✗ | ✗ | ✗ |

註記：
- `✔¹/✔²/✔³` = 金額級距上限 1/2/3 級（§4.4）。
- ACT-W5 簽收：簽收人原則是**個案本人**（不在角色表內）；訪視員代收須加註代收事由＋照片，屬補償控制。
- ACT-S3 的 `◐²`：司機（R-LOGI）**不得驗收自己送的那張單**（送貨者=驗收者是最基本的 SoD 破口），可驗收他人單。
- ACT-T4「限本人」：授權判斷不只看角色還看**身分綁定**（assignee===當前 uid），矩陣要支援 owner-scope。

### 4.4 級距軸

**金額（金援）** 〖三個門檻值待場次 B 定案，下為現行暫行〗
| 級 | 金額 | 最低核准位階 |
|---|---|---|
| 1 | ≤ $3,000 | R-LEAD（組長） |
| 2 | ≤ $10,000 | R-OFFICER（幹部） |
| 3 | > $10,000 | R-HQ（高層） |

規則：**核准人位階 ≥ 金額所需位階**才放行（把 S3 從顯示文字變成 `can()` 硬檢查）。審核（W2）可低一級：組長可審核幹部才能核准的案（初審與核決分層）。

**風險（任務）** 現行 `riskProfile: normal|high` 二級，暫行維持；正式分級表 = TODO-WORKSHOP-17 產出後替換為三級（一般/需裝備/專業隊專屬），矩陣格式已預留。

**個資敏感度（欄位分類）** — 這是把 F6 修好的核心：
| 級 | 定義 | 欄位例 | 誰可見（明文） |
|---|---|---|---|
| S0 | 作業必要 | 姓名（遮蔽形）、組別、任務 | 全員 |
| S1 | 內部 | 全名、年齡、性別、時段體能 | R-LEAD 以上＋任務相關人 |
| S2 | 敏感 | 電話、地址、Email、GPS 動線 | R-OFFICER 以上；訪視員限**自己負責的個案** |
| S3 | 特種 | 身分證、生日、**飲食過敏、心理狀態** | 〖個資權責人待定〗；例外：香積可見過敏欄（僅過敏欄）、訪視可見自己個案的心理欄 |

S3 的例外規則是關鍵設計：**「需要知道」比「位階夠高」優先**。高層不備餐，不需要看過敏名單；香積要備餐，需要。這就是 need-to-know 原則落到欄位級——位階給天花板，need-to-know 給實際鑰匙。

### 4.5 戰時差分表（只列與平時不同的格子）

〖每一列都是工作坊要高層點頭的「戰時放寬」，逐列表決〗

| 動作 | 平時 | 戰時 | 理由 | 補償控制 |
|---|---|---|---|---|
| ACT-W3 核准（R-LEAD） | 上限 $3k | 〖上限 $10k?〗 | HQ 可能失聯，現場要能發應急金 | 全案標「戰時放寬」，48h 內補審 |
| ACT-T3 高風險派工（R-OFFICER） | 覆核 confirm | 免二次確認 | 爭分奪秒 | 稽核加重＋事後彙報 |
| ACT-S2 物資派案（R-LEAD） | ✗ | ✔ | 倉管可能不在場 | 單張上限〖?〗 |
| ACT-X1 戰時解除（回平時） | admin/it | **僅 admin** | 解除比進入更該嚴（避免誤觸清空戰時狀態） | — |
| ACT-C4 讀 S2 個資（R-LEAD） | ✗ | ✔（自己轄區） | 斷網時班長要能聯絡個案 | 讀取留痕（脊椎 C） |
| master 資料編輯 | 各依矩陣 | 全鎖唯讀（現行已有） | 戰時單一資訊流 | — |

注意最後一列：**戰時不是全面放寬，有些格子反而收緊**（master 鎖定、解除戰時只留 admin）。差分表讓「放寬」和「收緊」都一目了然——這是整份複製的 PERMS_WAR 做不到的。

---

## 5. 代理與逾時升級

核准人失聯是災時常態，不是例外。規則設計借用任務池已驗證的「受阻釋放」模式：

1. **指定代理**：每個 R-OFFICER 以上角色可預先指定一名代理人〖工作坊：代理人資格？同位階？〗。代理期間所有動作稽核記「A 代 B」。
2. **逾時自動升級**：核准請求發出後 `T` 未處理 → 自動升給**上一位階**并通知原核准人。〖T 平時=24h？戰時=30min？〗升級後的核准稽核標「逾時升級」。
3. **升到頂沒人**：R-HQ 也失聯 → 進入破窗（§7），不是卡死。**系統永遠要有出口**，卡死的核可流程在災場會被整個繞過，那比放寬更危險——人會回到 LINE 私訊拍板，所有留痕歸零。這是本節最重要的設計理由：**流程要夠好用，稽核才會真實**。

## 6. 最小可行責任分離（SoD）＋補償控制

現實：小聯絡處只有 2-3 位幹部，教科書式 SoD（五步五人）不可行。分級處理：

| 級 | 條件 | 規則 |
|---|---|---|
| 硬 SoD | 任何規模都強制 | ①金援：**核准人 ≠ 申請人**。②物資：**驗收人 ≠ 送貨人**（4.3 註）。③破窗：**審批人 ≠ 申請人**。就這三條，再小的處都做得到 |
| 軟 SoD | 人力足夠時 | 五步鏈五人各異（現行軟提醒保留） |
| 補償控制 | 人力不足違反軟 SoD 時 | 同一人連續簽核 → (a) 系統強制第二通道通知上一位階（LINE 推播），(b) 稽核記錄加 `sodOverlap:true`，(c) 進入月報例外清單〖月報格式=場次 B 產出〗 |

現行 `advanceWelfare` 的 confirm 軟提醒升級為上述三件套。**重點：不阻止（阻止=實務上做不到=被繞過），但讓違反「自動變得可見」。**

## 7. 破窗機制（Break-glass）正式化

現行 `requestPerm/approvePerm` 已是原型（30 秒自動回收、金鑰、留痕），但目標寫死 staff×vol_hub。泛化為：

```
breakGlass(actor, action, targetCtx, reason)
  → 建立破窗請求（reason 必填，不可空字串）
  → 推播給有 ACT-X3 權限的所有人
  → 核准後：授予【該動作×該對象】的一次性權限，TTL=〖平時 30min / 戰時 2h?〗
  → 到期自動回收（沿用現行 setTimeout 模式；後端上線後改伺服器 TTL）
  → 全程寫入稽核：請求/核准/使用/回收 四筆事件，鏈上 breakGlassId
```

破窗與代理升級（§5）的分工：**代理升級處理「人不在」，破窗處理「格子是 ✗ 但情況特殊」**。兩者都不修改基準矩陣——矩陣永遠反映常態，例外走例外通道。這樣稽核月報只要看兩張清單（破窗清單＋SoD 例外清單）就掌握全部異常。

## 8. 實作規格：`can()` 單一守門函式

```js
// 唯一授權入口。回傳 {ok:boolean, reason:string, compensate?:string[]}
// ctx: {amount?, risk?, piiClass?, ownerUid?, scope?, state}  // state 由系統注入
function can(actor, actionId, ctx){ … }
```

- 矩陣本體為資料（`AUTH_MATRIX` 常數 + 戰時 delta 物件），不是程式邏輯。工作坊改門檻=改資料，不動程式。
- **fallback 一律 P4/拒絕**（修 F1：default-deny）。
- 每次 `ok:false` 與每次 `compensate` 觸發，都發一筆稽核事件（脊椎 C 的 `AUTH_DENIED` / `AUTH_COMPENSATED` 事件型）。
- `renderNav` 與 `showPage` 都改讀 `can()`（修 F2：同一張表，答案必然一致）。
- LOA 端六角色經 §4.1 映射表換算成統一角色後走同一個 `can()`（修 F3）。

### 8.1 重構點清單（散落 → 引用矩陣）

| 現行 | 改為 | 順帶修掉 |
|---|---|---|
| `canViewFullPII()` | `can(actor,'ACT-C4',{piiClass})` | F6 二值→分級；diet/psych 納管 |
| `canAssignCase()` | `can(actor,'ACT-C2')` | — |
| `advanceWelfare` 內 vol 檢查 | `can(actor,'ACT-W'+n,{amount})` | F4 級距強制化 |
| `welfareApprovalLevel()` | 矩陣級距資料的顯示器（保留為 UI 說明） | — |
| `rtGuardHighRiskAssign()` | `can(actor,'ACT-T3',{risk})` 的 compensate 路徑 | — |
| `setS()` / `toggleEditMode()` / `checkAdminGate()` / `cycleP()` / `dispatchDynamicRoutePermission()` | 各自 `can(actor,'ACT-X*')` | — |
| `dispatchReq()` / `verifyDelivery()` | `can(actor,'ACT-S2/S3',{…})` | **F5 補門**；驗收記 actor uid |
| `renderNav` 的 PERMS_PEACE 直讀 | `can()` | **F2** |
| `(…)||'P1'` fallback | `can()` 內 default-deny | **F1** |
| `NAV_CFG[].roles` | 刪除（死資料） | F-S14 |
| `requestPerm/approvePerm` | `breakGlass()`（§7） | S12 寫死目標 |

遷移策略：`can()` 先落地為薄殼（內部暫時翻譯成現行規則，行為零變化）→ 各呼叫點逐一切換 → 最後把矩陣資料填入、刪舊函式。每一步可獨立驗證（Playwright harness 已有先例）。

---

## 9. 場次 B 待確認清單（工作坊白板直接用）

1. 〖金額三門檻〗$3,000 / $10,000 的線畫對嗎？各聯絡處一致還是可各自設定？
2. 〖戰時放寬表〗§4.5 每一列逐項表決，尤其組長戰時核准上限。
3. 〖IT 的業務權限〗it 是否保留看完整個資/核金援的能力？（建議：否，走破窗）
4. 〖個資權責人〗S3 特種欄位（身分證/過敏/心理）明文讀取權給誰？一人還是一崗？
5. 〖訪視/香積角色〗要不要成為儀表板正式角色？
6. 〖破窗審批圈〗ACT-X3 只給 R-HQ，還是 R-OFFICER 也可（互批）？
7. 〖代理與逾時〗代理人資格、平時/戰時逾時門檻 T。
8. 〖跨聯絡處〗大災跨處支援時，外處幹部在本處的位階映射？（連動 TODO-12/13，可延後）

## 10. 跨脊椎連結點

- **→ 脊椎 A（後端）**：`can()` 是未來伺服器端驗證的同一份規格；前端矩陣屆時降級為 UX 提示。矩陣資料放後端下發，前端快取（離線可用，回線校驗）。
- **→ 脊椎 C（稽核）**：`AUTH_DENIED` / `AUTH_COMPENSATED` / `BREAK_GLASS_*` / `SOD_OVERLAP` 四類事件是稽核模型的必備事件型；破窗 TTL 回收也要留痕。**授權矩陣沒有稽核=沒有牙齒；稽核沒有矩陣=不知道什麼算異常。B 和 C 是一對。**
- **→ 脊椎 D（飛輪）**：演練模式下的 `can()` 拒絕/破窗紀錄，是演練評分的現成指標（「這一梯破窗了幾次」= 授權設計與現實的落差計量），回饋修正矩陣門檻。

---

## 附：本文件如何維護

- 門檻值、放寬表 = **資料**，場次 B 後改這裡＋`AUTH_MATRIX` 常數，一次同步。
- 新增功能時：先在 §4.2 動作目錄登記 ACT ID，再寫程式。**沒有 ACT ID 的守門邏輯 = 回到散落時代**，code review 據此把關。
