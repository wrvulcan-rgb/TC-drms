# FABLE 交接 Brief — TC-DRMS 待辦連結性與多層次思考地圖

> **這份文件的用途**：讓 Fable（或任何接手的長脈絡 session）能**冷啟動全速思考**。
> 它不是規格書，是**思考的地圖 + 燃料**。每一節都刻意往下挖到「你可能還沒想到」的層次，
> 並在每根脊椎末尾附上「可以直接開跑的 Fable 任務指令」。
>
> **給 Fable 的一句話**：不要把下面的待辦當成平行清單逐項做。先認出它們共用的 **4 根脊椎**，
> 沿著脊椎思考，你會自動發現大量二階、三階的細節。放膽往深處走，把作者沒寫到的那層補出來。
>
> 更新於 2026-07-04 · 對應 branch `claude/backlog-review-bugs-okcvip` / `main`

---

## 0. 先讀這段：系統現況與你要保護的東西

**TC-DRMS 是什麼**：慈濟災害應變管理系統，單檔前端（`index.html` + `app.js` ~8900 行 + `style.css`）。
平時/戰時雙模式，涵蓋報到、即時調度、個案陪伴、物資、金援、監控。**目前沒有後端**，
資料在瀏覽器 `localStorage`，跨分頁靠 `BroadcastChannel` 同步。`RTDB` 物件模擬 Firebase 介面
（`RTDB.ref(path).on/set/update/push`），換真實 SDK 可無痛切換。

**最近已完成（這些是後續思考的地基，別重做）**：
- 任務受派確認（`待派工 → 待確認 → 進行中 → 已完成`；`rtDoAssign/rtAcceptTask/rtRejectTask`）
- 物資到場驗收（`待派案→已派案→配送中→待驗收→已送達`；`verifyDelivery` 清點+差異回報）
- 交接班自動彙整（`_pushHandoverSnapshot` → RTDB `handover`）
- 金援五步審核鏈（`WELFARE_STAGES`、`applyWelfare/advanceWelfare`、`welfareChain`、責任分離軟提醒、`welfareApprovalLevel`）
- 個案負責人指派（`canAssignCase/doAssignCase`、寫 timeline + 推播）
- 時間戳統一（`fmtTS()/fmtTSSec()`，修掉 lossy `MM-DD`）
- 個資遮蔽層（`maskPII/canViewFullPII`，null-safe，含 id/phone/email/name/address）

**你要保護的三條紅線（任何設計都不能違反）**：
1. **離線韌性優先**：救災現場網路最不穩，偏偏最需要系統。任何後端設計都必須 offline-first。
2. **演練 ≠ 實戰**：`_drillMode` / URL `?drill=1` 的演練資料，**永遠不得污染**真實金援、個案、稽核紀錄。
3. **輔助角色定位**：慈濟是全輔助角色，一般志工不得派入高風險現場（`rtGuardHighRiskAssign` 已守門）。

---

## 脊椎 A：「有沒有後端」是資安三題的同一個根

系統管理頁三個 P1/P2 待辦，是**同一決策的三個症狀**：個資明文（無存取層）、前端可竄改（無驗證）、
無法多人協作（無伺服器）。四個 API 串接（Firebase / LINE / GAS / Drive）也全掛在這根脊椎上。

### 往下挖的層次

- **L1 表面**：架後端、加密、加驗證。
- **L2 真正的約束是離線**：這不是普通 client-server。網路斷的當下正是救災高峰。
  - Firebase RTDB 有 offline persistence + 自動 sync；自架則要 service worker + 本地寫入佇列 + 衝突解。
  - 必須定義「降級模式」：後端斷線時哪些能讀（快取）、哪些能寫（本地佇列，回線補送）、哪些直接禁止。
- **L2 個資法/資料落地**：台灣個資法 —— 資料實體存哪？Google（境外機房）vs 基金會本地伺服器，
  牽涉跨境傳輸與權責人。**選型決策無法脫離法遵**，這是純技術會漏掉的。
- **L2 威脅模型要分層**（紅隊清單的骨架）：
  1. 隨手竄改（志工開 console 改 role）
  2. 內部濫用（幹部看了不該看的個資）
  3. 外洩（Sheets 連結被轉傳）
  4. **供應鏈**：`index.html` 目前從 `unpkg`（Leaflet）與 Google Fonts 載入外部資源 —— CDN 被汙染 = XSS。
     這是一個**真實且被忽略的攻擊面**，救災工具尤其不該有。建議自帶資產（self-host / 內聯）。
- **L3 認證的兩難**：志工目前靠 QR 報到，QR 可偽造/重放。要簽章 token，但**現場志工可能沒智慧型手機/帳號**。
  安全 vs 低摩擦入場，是根本張力 → 可能要「分級身分」：訪客級（僅報到）/ 具名級（可操作）/ 幹部級（可核准）。
- **L3 金鑰管理**：若做欄位級加密，金鑰放哪？純前端加密 = 金鑰分發問題。可能需要「後端持金鑰、前端只拿遮蔽視圖」。
- **L3 韌性工程**：容器是 ephemeral（見執行環境），真實部署要備份/DR 策略、資料版本遷移
  （注意：`RTDB.seed` 只在空時 seed，**schema 演進後舊 localStorage 會有陳舊欄位** → 潛在 bug 類，需 migration 層）。

### 給 Fable 的任務指令（A）
> 寫一份《後端架構決策書》，把資安三題 + 四個 API 當成**同一決策的驅動力**。
> 必含：offline-first 策略（降級模式矩陣）、Firebase vs 自架的法遵×成本×維運三軸比較、
> 分階段上線路徑（每階段可獨立出貨）、供應鏈加固（移除 CDN 依賴）、schema migration 層設計。
> 明確標出「只有人能拍板」的參數（預算、維運人力、資料落地政策）並 bracket 起來。

---

## 脊椎 B：授權矩陣是橫切整個系統的隱形骨架

「誰可以做什麼」現在**散落在七八處各寫各的**：`canViewFullPII()`、`welfareApprovalLevel()`、
`canAssignCase()`、`PERMS_PEACE/PERMS_WAR`、高風險任務守門、物資驗收權…… 全是**同一張表的投影**。
它也正是工作坊 **TODO-09 授權矩陣**的產出。

### 往下挖的層次

- **L1 表面**：一張 `角色 × 動作 × 級距` 表。
- **L2 它其實是三維的**：系統已有 `PERMS_PEACE` / `PERMS_WAR`。**戰時授權要下放**（HQ 被淹沒，組長臨場能核更多）。
  → 矩陣要含「情境維度」：平時嚴、戰時鬆 + 事後補審。金額級距也隨情境變（戰時組長上限拉高換速度）。
- **L2 授權者不在場怎麼辦**：核准人在災區/斷網 —— 需要**代理核准鏈 + 逾時自動升級**。
  這正是任務池「受阻釋放」`rtBlockedRelease` 的模式，套到核准流程。
- **L2 責任分離的現實張力**：小聯絡處可能只有 2-3 個幹部，嚴格 SoD 根本做不到。
  → 需要「最小可行分離」+ 補償控制（同一人時，強制第二管道通知高層 + 加重稽核）。我已埋軟提醒，需正式化。
- **L3 破窗機制（break-glass）**：緊急越權 + 重稽核。系統已有臨時提權（`requestPerm`，30 秒自動回收）可泛化。
- **L3 角色 vs 個人**：現在 role 是下拉（admin/staff/…），真實組織要「某人就此事件代理某角色」。矩陣需支援 person-level 委派。
- **L3 跨聯絡處維度**（TODO-12/13）：跨區支援時誰能核准/調度別區資源。矩陣還有一條「組織層級」軸。
- **L3 個資敏感度分級**：不是所有 PII 等值。身分證 > 電話 > 姓名。資料分級 × 存取分級交叉。`maskPII` 已是雛形，需接上矩陣。

### 給 Fable 的任務指令（B）— **建議第一個做，無前置、可直接餵工作坊場次 B**
> 逆向收斂全系統散落的權限規則，寫成**一張主授權矩陣**：維度 = 角色 × 動作 × 級距(金額/風險/個資敏感度) × 情境(平時/戰時) × 組織層級。
> 附：代理/逾時升級規則、最小可行責任分離 + 補償控制、break-glass 流程、各模組「改成引用同一張表」的重構點清單。
> 產出要能當場次 B 的討論白板底稿（保留待確認欄位）。
>
> **✅ 已交付 → `AUTH_MATRIX_SPEC.md`**（2026-07-05）。盤點出 22 處散落規則與 6 個結構性發現
> （F1 default-open fallback、F2 導覽/守門讀不同表、F3 三套角色詞彙未映射、F4 金援級距未強制、
> F5 物資動線無門、F6 個資遮蔽二值 vs 特種欄位 diet/psych 未納管）。
> 後續：①場次 B 拿 §9 待確認清單表決 ②依 §8.1 重構點清單實作 `can()`。

---

## 脊椎 C：稽核軌跡已很多，但四散

系統已有**四套留痕**：`auditLog`（`rtAudit`）、`timeline`（個案）、`reportLog`（事件流）、
`reliefLog.chain`（金援五步）。個資待辦要「存取留稽核」、紅隊要證明「誰做了什麼」—— 都靠這條線。

### 往下挖的層次

- **L1 表面**：統一成一個稽核事件模型 `{id, ts, actor, actorRole, action, targetType, targetId, before, after, channel, incidentId, drill}`。
- **L2 防竄改**：稽核紀錄若操作者能改就毫無意義（對「偽造結案」威脅）。
  → **append-only + 雜湊鏈**（每筆 hash 前一筆），即使沒有後端也能「事後偵測竄改」。這是聰明的半成品解，client-side 可先做。
- **L2 演練污染硬隔離**：已有 `drill` 旗標，但**金援/財務類稽核，演練事件必須完全隔離**（法律/財務不可混）。
- **L2 稽核本身含個資**：誰對誰做了什麼 = PII。稽核**檢視**也要遮蔽 + 稽核（誰讀了稽核紀錄）。保存期限政策。
- **L3 它是 C↔D 的接點**：稽核事件流就是 `genStoryline` 的原料，也是脊椎 D 訓練資料的原料。**統一稽核模型 = 同時餵故事線與 AI**。這是最不明顯但最有價值的連結。
- **L3 兩種消費者要分流**：使用者動作稽核 vs 系統遙測（`SESSION.errors` / `rapidClicks` 已存在，是 UX/穩定度用途，不是責任歸屬用途）。別混。

### 給 Fable 的任務指令（C）
> 設計統一稽核事件模型，讓四套現有機制往它匯流；含防竄改雜湊鏈（client-side 版）、演練硬隔離、
> 稽核檢視的個資遮蔽、保存期限、以及「稽核流如何同時餵 genStoryline 與訓練資料」的接口設計。

---

## 脊椎 D：資料飛輪（結案 → 演練 → AI）

`genStoryline/exportStoryline`（已有）→ 演練劇本（TODO-16）→ 歷史數位化（TODO-15）→ AI 媒合（Phase 4）。
四個待辦是**一個飛輪**：今天的軌跡 = 明天的演練 = 後天訓練 AI 的燃料。

### 往下挖的層次

- **L1 表面**：結案報告匯出成演練劇本。
- **L2 隱私悖論**：要用真實歷史訓練 AI，那正是你要保護的個資 → 需**匿名化/合成資料**管線。直接連回脊椎 A/B。
- **L2 燃料品質決定飛輪**：Garbage in = garbage out。飛輪只在**結案資料完整**時才轉得動 ——
  而這正是最近完成的流程完整性工作（任務確認/物資驗收/金援鏈都產出結構化紀錄）的價值。**近期的工作就是飛輪的燃料源**。
- **L2 難度分級**（TODO-16 要三級）：什麼叫「難」？`斷網 + 人力不足 + 高風險任務`同時發生。
  系統的「變化球」機制（`overflow/shortage/supplies`）已在模擬擾動，可延伸成難度旋鈕。
- **L3 模擬保真度**：演練應能**重放真實事件**。`exportStoryline` → 匯入為 drill scenario 的來回閉環（round-trip）。
- **L3 媒合引擎的前置**：`matchengine`（技能匹配 30%）需要能力分類法（TODO-01/02/03，人力工作坊）→ D 依賴人的一側。
- **L3 冷啟動**：現在沒歷史。用演習資料 + 3-5 份歷史紙本案例（TODO-15）bootstrap。

### 給 Fable 的任務指令（D）
> 寫《資料飛輪設計》：結案→演練→AI 的閉環、匿名化/合成資料管線、難度分級旋鈕（接變化球機制）、
> exportStoryline↔drill 的 round-trip 格式、冷啟動策略。標出對人力工作坊（能力分類）的依賴。

---

## 橫切關注點（Fable 請一併掃描，這些不屬於單一脊椎，但會咬到全部）

- **壓力/高齡 UX**：志工橫跨年齡層，現場高壓。`簡易大字模式`已在，把「防呆 + 無障礙」當一級約束，不是事後加。
- **Schema 版本遷移**：`localStorage` 舊資料 × 新欄位（我近期加了 `welfareChain/qtyDiff/verifiedQty/待確認狀態`）→ 需遷移層，否則老使用者重整後行為不一致。**這是現在就潛伏的 bug 類。**
- **測試策略**：目前零自動化測試（我一直用臨時 Playwright 驗證）。建議建一個回歸 harness（`file://` + `page.evaluate` 打純函式）。
- **術語一致性**：慈濟用語（師兄/師姐/香積/法親/聯絡處）vs 通用詞。建一份 glossary，避免各模組不一致。
- **治理風險（請 Fable 明講）**：`CLAUDE.md` 有「push feature branch 後自動 merge main」規則 —— 這等於**無審查直上 production**，對一個含個資的救災系統是治理風險，值得建議加 review gate。
- **時間/時區**：`fmtTS` 用本地時間；全台灣無跨時區問題，但 drill 跨日重放要小心。

---

## 只有「人」能回答的岔路（Fable 請 bracket，不要自己假設）

1. 資料落地：Google 境外 vs 基金會本地伺服器？（法遵 + 預算）
2. 維運人力：有沒有專責 IT 能顧自架伺服器？沒有的話 Firebase 幾乎是唯一解。
3. 授權天花板：各金額級距對應到哪個實際職稱？戰時下放到什麼程度？
4. 個資權責人：依個資法，誰是完整個資的唯一可讀角色？
5. 志工身分嚴謹度：要不要每個志工有帳號？還是訪客級 QR 就好？
6. 歷史資料授權：舊個案資料能不能拿來訓練 AI？家屬同意權？

---

## 建議的 Fable 撰寫順序

| 順序 | 脊椎 | 為何這個順序 | 前置 |
|---|---|---|---|
| 1 | **B 授權矩陣** | 無前置、可直接餵工作坊場次 B、是 A/C 的共同依賴 | 無 |
| 2 | **C 稽核模型** | 接在 B 後（動作定義好才好記誰做了什麼）；且是 D 的原料 | B |
| 3 | **A 後端決策** | 需人先給預算/維運/落地三參數 | 人拍板 |
| 4 | **D 資料飛輪** | 最前瞻、依賴人力工作坊 | C + 人力工作坊 |

---

## 給 Fable 的收尾提示

- 放膽往三階效應走。作者（前一個 session）只挖到二階，**你的價值在補第三階與跨脊椎的隱藏連結**。
- 每寫一份，回頭檢查是否咬到另一根脊椎 —— 若有，明確標註連結點（例：稽核模型的 `before/after` 就是飛輪的訓練 label）。
- 遇到「只有人能回答」的參數，不要猜，列進上面第 6 節，繼續往下寫其他能定的部分。
- 守住三條紅線（離線優先 / 演練不污染實戰 / 輔助角色定位）。任何違反的設計直接標紅。
- 導覽用的關鍵符號都在 `app.js`：`RTDB`、`PERMS_PEACE`、`WELFARE_STAGES`、`maskPII`、`rtAudit`、
  `_pushHandoverSnapshot`、`genStoryline`、`_drillMode`、`SESSION`、`fmtTS`。從這些點進去可摸到整個系統。
